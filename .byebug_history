exit
@artist = Artist.new(params.require(:artist).permit(:name, :bio))
    @student = Student.new(params.require(:student).permit(:name, :bio))
@artist = Artist.new(params(:artist))
@artist = Artist.new(params.require(:artist))
params
@artist
